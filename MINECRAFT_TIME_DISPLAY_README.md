# ⏰ Отображение времени игрового мира в Minecraft

## 📋 Описание

Реализована система отображения времени игрового мира в правом углу экрана Minecraft через scoreboard.

## 🎯 Что было сделано

### 1. Datapack для автоматического обновления времени

Создан datapack в папке `server/mcraft/world/datapacks/gametime_display/`, который:
- Автоматически загружается при запуске сервера
- Обновляет время каждые 20 тиков (1 раз в секунду)
- Отображает время в правом углу экрана через scoreboard

**Структура datapack:**
```
server/mcraft/world/datapacks/gametime_display/
├── pack.mcmeta
├── data/
│   ├── gametime_display/
│   │   └── functions/
│   │       ├── init.mcfunction      # Инициализация при загрузке
│   │       ├── tick.mcfunction      # Вызывается каждый тик
│   │       └── update_time.mcfunction  # Обновление времени
│   └── minecraft/
│       └── tags/
│           └── functions/
│               ├── tick.json        # Тег для вызова каждый тик
│               └── load.json        # Тег для вызова при загрузке
└── README.md
```

### 2. Node.js сервис (резервный вариант)

Создан сервис `server/services/minecraftTimeService.js`, который:
- Может обновлять scoreboard через команды сервера
- Используется как резервный вариант, если datapack не работает
- Автоматически запускается при старте сервера

### 3. Интеграция в minecraft-server.js

Сервис времени автоматически запускается при старте сервера и останавливается при его остановке.

## 🚀 Использование

### Автоматическая активация

При запуске сервера datapack автоматически загрузится и начнет работать. Время будет отображаться в правом углу экрана для всех игроков.

### Ручная активация (если нужно)

Если datapack не загрузился автоматически, выполните в игре:
```
/reload
```

Или в консоли сервера:
```
reload
```

### Что отображается

В правом углу экрана будет показан scoreboard с:
- **⏰ Часы** - текущий час игрового дня (0-23)
- **⏰ Минуты** - текущие минуты (0-59)
- **⏰ Время дня** - общее время дня в игровых тиках (0-24000)

### Формат времени

- **0-6000**: Утро (6:00 - 12:00)
- **6000-12000**: День (12:00 - 18:00)
- **12000-18000**: Вечер (18:00 - 0:00)
- **18000-24000**: Ночь (0:00 - 6:00)

## 🛠️ Управление отображением

### Временно скрыть scoreboard

В игре:
```
/scoreboard objectives setdisplay sidebar
```

### Снова показать scoreboard

В игре:
```
/scoreboard objectives setdisplay sidebar gametime_display
```

### Полностью удалить scoreboard

В игре:
```
/scoreboard objectives remove gametime_display
```

## 🔧 Технические детали

### Как работает datapack

1. При загрузке мира вызывается `init.mcfunction`
2. Создается scoreboard `gametime_display`
3. Scoreboard настраивается для отображения справа (sidebar)
4. Каждый тик вызывается `tick.mcfunction`, который запускает `update_time.mcfunction`
5. `update_time.mcfunction` получает текущее время через `time query daytime`
6. Вычисляются часы и минуты
7. Обновляются значения в scoreboard

### Формулы вычисления

- **Часы**: `(время / 1000) % 24`
- **Минуты**: `((время % 1000) / 1000) * 60`

Где `время` - это значение от 0 до 24000 (игровые тики).

## 📝 Примечания

- Datapack работает автоматически, не требует дополнительной настройки
- Время обновляется каждые 20 тиков (1 раз в секунду)
- Scoreboard виден всем игрокам на сервере
- Если datapack не работает, Node.js сервис может обновлять время через команды (но это менее эффективно)

## 🐛 Устранение неполадок

### Время не отображается

1. Проверьте, что datapack загружен:
   ```
   /datapack list
   ```
   Должен быть виден `[file/gametime_display]`

2. Перезагрузите datapack:
   ```
   /reload
   ```

3. Проверьте, что scoreboard создан:
   ```
   /scoreboard objectives list
   ```
   Должен быть виден `gametime_display`

4. Убедитесь, что scoreboard отображается:
   ```
   /scoreboard objectives setdisplay sidebar gametime_display
   ```

### Время обновляется неправильно

- Проверьте логи сервера на наличие ошибок
- Убедитесь, что используется правильная версия Minecraft (1.21.10)
- Попробуйте перезагрузить datapack: `/reload`

## ✅ Готово!

Теперь время игрового мира отображается в правом углу экрана для всех игроков! 🎉

